apiVersion: v1
kind: Service
metadata:
  name: processor
  namespace: {{ .Values.namespace.name }}
  labels:
    app: processor
spec:
  selector:
    app: processor
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080