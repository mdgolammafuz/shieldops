apiVersion: v1
kind: Service
metadata:
  name: ingestor
  namespace: {{ .Values.namespace.name }}
  labels:
    app: ingestor
spec:
  selector:
    app: ingestor
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080